import React from 'react';
import { makeStyles } from '@material-ui/core';


export const HelpButtonStyles = makeStyles({
    helpButton: {
        padding: '0.7em 1.5em',
        fontWeight: 800,
        borderRadius: '3em',
        textAlign: 'center',
        float: 'right',
    },
});
