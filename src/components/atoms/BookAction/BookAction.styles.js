import React from 'react';
import { makeStyles } from '@material-ui/core';

export const BookActionStyles = makeStyles((theme) => {
    return {
        root: {
            width: '100%',
            borderRadius: '5%',
            margin: 0,
            fontWeight: 600,
            fontSize: '1rem',
        }
    }
});
